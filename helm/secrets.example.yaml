---
# Copiar a secrets.yaml (NO commitear) y completar con claves reales.
# Instalacion: helm upgrade --install doctor-ku ./helm \
#   -f helm/values-production.yaml -f helm/secrets.yaml

secrets:
  sshKeys:
    default: |
      # -----BEGIN OPENSSH PRIVATE KEY-----
      # reemplazar con tu clave privada SSH (acceso a nodos k3s)
      # -----END OPENSSH PRIVATE KEY-----
